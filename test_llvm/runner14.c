/* Half 3 of the module suite.
**
** runner12.c holds every test; the library plus all of them do not fit in
** one PRG. This selects another third and reuses the same source, so no
** test or helper is ever duplicated. See the note at the top of
** runner12.c.
*/
#define SUITE 3
#include "runner12.c"
